/// @brief Key to the context used for communication within the 
/// ThreeDView obect hierarchy. Each node of the rendering tree
/// is expected to have its own context, modifying the parent's 
/// to allow proper branching.
export const key = {};